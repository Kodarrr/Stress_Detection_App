<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"

    android:background="@drawable/background_image">

    <LinearLayout
        android:id="@+id/appname"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="0dp">

        <ImageView
            android:id="@+id/back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:src="@drawable/baseline_arrow_back_ios_24"
            android:transitionName="logo_image" />

        <ImageView
            android:id="@+id/iv2"
            android:layout_width="165dp"
            android:layout_height="50dp"
            android:layout_marginLeft="10dp"
            android:src="@drawable/app_logo"
            android:transitionName="logo_image" />
    </LinearLayout>

    <!-- Battery Status Card (unchanged) -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        android:background="@android:color/white">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- Battery Status Title -->
            <TextView
                android:id="@+id/statusTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Battery Status"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#2F7D32" />

            <!-- Settings Icon -->
            <ImageView
                android:id="@+id/settingsIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_alignParentEnd="true"
                android:layout_marginEnd="8dp"
                android:src="@android:drawable/ic_menu_preferences" />

            <!-- Battery Percentage -->
            <TextView
                android:id="@+id/batteryStatusTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/statusTextView"
                android:text="39%"
                android:textSize="36sp"
                android:textStyle="bold"
                android:textColor="#2F7D32"
                android:layout_marginTop="8dp"/>

            <!-- Battery Details -->
            <TextView
                android:id="@+id/batteryDetailsTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/batteryStatusTextView"
                android:text="Charging USB\nCurrent 132 mA\nPower 0.5 W"
                android:textSize="14sp"
                android:textColor="#666"
                android:layout_marginTop="4dp"/>
        </RelativeLayout>
    </androidx.cardview.widget.CardView>

    <!-- Do Not Disturb Status Card (unchanged) -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        android:background="@android:color/white">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- DND Status Title -->
            <TextView
                android:id="@+id/dndTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Do Not Disturb"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#2F7D32" />

            <!-- DND Icon (Optional) -->
            <ImageView
                android:id="@+id/dndIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_alignParentEnd="true"
                android:layout_marginEnd="8dp"
                android:src="@android:drawable/ic_lock_silent_mode" />

            <!-- DND Status -->
            <TextView
                android:id="@+id/dndStatusTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@id/dndTextView"
                android:text="Off"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="#666"
                android:layout_marginTop="4dp"/>
        </RelativeLayout>
    </androidx.cardview.widget.CardView>

    <!-- New Battery Level Bar Chart Card -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        android:background="@android:color/white"
        android:layout_marginTop="12dp">

        <com.github.mikephil.charting.charts.BarChart
            android:id="@+id/batteryChart"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"/>

    </androidx.cardview.widget.CardView>

</LinearLayout>